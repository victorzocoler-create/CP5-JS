{
  "name": "gourmeton-project",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:all": "npm install && npm install --prefix frontend && npm install --prefix api",
    "dev": "npx concurrently \"npm run dev --prefix frontend\" \"npm run dev --prefix api\"",
    "build": "npm run install:all && cd frontend && npm run build && cd .. && rm -rf dist && mv frontend/dist ./dist",
    "start": "npm run start --prefix api"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
